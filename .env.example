# Base path for root API (browser). With Vite dev server use /api so requests are same-origin and cookies work (see vite.config.ts proxy).
VITE_ROOT_API_URL=/api

# Port where the root microservice listens (used only by Vite dev proxy in vite.config.ts).
VITE_PROXY_ROOT_PORT=3000
