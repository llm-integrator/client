# Base URL for the root API in the browser (single source of truth).
# Local dev (Vite proxies this path to localhost:3000 — see vite.config.ts): VITE_AP_URL=/api
# Production or direct backend URL: VITE_AP_URL=https://api.example.com
VITE_AP_URL=/api
