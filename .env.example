# Base URL for the root API in the browser (single source of truth).
# Local dev (Vite proxies this path to localhost:3000 — see vite.config.ts): VITE_API_URL=/api
# Production or direct backend URL: VITE_API_URL=https://api.example.com
VITE_API_URL=http://localhost:3000
